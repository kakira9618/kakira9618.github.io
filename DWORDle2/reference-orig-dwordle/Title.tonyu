var titleText;
var cregitText;
var versionText;
var noticeText;

\onAppear() {
    // title text
    titleText = new StyledLabel{
        x: $screenWidth / 2,
        y: 120 * $screenP,
        size: 70 * $screenP,
        text: "DWORDle",        
        fontName: "Helvetica Neue",
        style: "bold",
        align: "center middle",
        lifeKeeper: this,
        zOrder: 1
    };
    
    // cregit
    cregitText = new Label{
        x: $screenWidth / 2,
        y: 170 * $screenP,
        size: 20 * $screenP,
        text: "by @kakira9618",
        align: "center middle",
        lifeKeeper: this,
        zOrder: 1
    };
    
    // version text
    versionText = new Label {
        x: $screenWidth,
        y: $screenHeight,
        size: 20 * $screenP,
        template: "version $version",
        align: "right bottom",
        lifeKeeper: this,
        zOrder: 1
    };
    
    // notice text
    noticeText = new Label {
        x: $screenWidth / 2,
        y: $screenHeight / 2,
        size: 20 * $screenP,
        template: "if you see this, \nplease reload page",
        align: "middle center",
        lifeKeeper: this,
        zOrder: 1
    };
}

while (true) {
    update();
}